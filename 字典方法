1
`d.get(key, default = None)`
返回字典中键 `key` 对应的值，如果没有这个键，返回 `default` 指定的值（默认是 `None` ）。

d.getdefaultkey(key[,default=none])
get基础上再在原dict中加一个键值对
2
`d.pop(key, default = None)`
删除并返回字典中键 key 对应的值，如果没有这个键，返回 default 指定的值（默认是 None ）。

3
与列表一样，del 函数可以用来删除字典中特定的键值对
del a["one"]

4
`d.update(newd)`
将字典newd中的内容更新到d中去。
注：可以通过索引来插入、修改单个键值对，但是如果想对多个键值对进行操作，这种方法就显得比较麻烦，好在有 update 方法：

5
in 可以用来判断字典中是否有某个特定的键
'chickens' in barn

6
`d.keys()` 
返回一个由所有键组成的列表；

`d.values()` 
返回一个由所有值组成的列表；

`d.items()` 
返回一个由所有键值对元组组成的列表；

7
d.clear()
清空所有键值 但字典本身还存在
